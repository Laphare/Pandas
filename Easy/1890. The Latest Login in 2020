import pandas as pd

def latest_login(logins: pd.DataFrame) -> pd.DataFrame:
    logins=logins.query("time_stamp.dt.year == 2020")
    df = logins.groupby("user_id")["time_stamp"].max().reset_index(name="last_stamp")
    return df
