import pandas as pd

def find_products(products: pd.DataFrame) -> pd.DataFrame:
    c1 = products["low_fats"].eq("Y")
    c2 = products["recyclable"].eq("Y")
    return products[c1 & c2][["product_id"]]
