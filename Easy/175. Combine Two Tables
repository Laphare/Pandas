import pandas as pd

def combine_two_tables(person: pd.DataFrame, address: pd.DataFrame) -> pd.DataFrame:
    x = pd.merge(person,address, on = "personId",how="left")
    return x[["firstName","lastName","city","state"]]
