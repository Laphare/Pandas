WITH max_list as (select customer_id, MAX(amount) AS max_amount
from online_store_orders
GROUP BY customer_id)

SELECT m.customer_id,c.customer_name
FROM max_list m
INNER JOIN online_store_customers c
ON c.customer_id = m.customer_id
WHERE m.max_amount >100
