import pandas as pd

def last_passenger(queue: pd.DataFrame) -> pd.DataFrame:
    queue = queue.sort_values("turn")
    queue["total"] = queue["weight"].cumsum()
    return queue.query("total<= 1000").iloc[[-1]][["person_name"]]
